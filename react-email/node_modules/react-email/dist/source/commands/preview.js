"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.startPreview = exports.buildPreview = void 0;
const fs_1 = __importDefault(require("fs"));
const shelljs_1 = __importDefault(require("shelljs"));
const utils_1 = require("../utils");
const run_server_1 = require("../utils/run-server");
const buildPreview = async ({ dir }) => {
    try {
        if (fs_1.default.existsSync(utils_1.REACT_EMAIL_ROOT)) {
            await (0, run_server_1.setupServer)('build', dir, '');
            return;
        }
        await (0, utils_1.downloadClient)();
        await (0, run_server_1.setupServer)('build', dir, '');
    }
    catch (error) {
        console.log(error);
        shelljs_1.default.exit(1);
    }
};
exports.buildPreview = buildPreview;
const startPreview = async ({ port }) => {
    try {
        if (fs_1.default.existsSync(utils_1.REACT_EMAIL_ROOT)) {
            await (0, run_server_1.setupServer)('start', '', port);
            return;
        }
        await (0, utils_1.downloadClient)();
        await (0, run_server_1.setupServer)('start', '', port);
    }
    catch (error) {
        console.log(error);
        shelljs_1.default.exit(1);
    }
};
exports.startPreview = startPreview;
